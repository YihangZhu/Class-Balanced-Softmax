#trainer:
#  checkpoint: 'saved/results_for_report/2024-08-03_12-06-39_reslt152_place_lt_0_gpus4/ckps/last_ckp.tar'
#  mode: 'validate'  # training or validation

representation:
  name: 'reslt152'
  args:
    after_1x1conv: True

classifier:
  args:
    tau_norm: 0.8
    linear_bias: True

loss:
  name: 'ResltLoss'
  args:
    beta: 0.99 # considering the parameter for imagenet-lt. the author did not public the values






